# 视频文件目录

请将作品视频放在此目录，文件名需与 `src/data/works.ts` 中的 `videoSrc` 字段一致。

## 当前作品对应的视频文件

| 文件名 | 作品 |
|--------|------|
| `baabaabet.mov` | 咩咩贝特 |

## 说明

- 推荐使用 H.264 编码的 MP4，兼容性最好；也支持 MOV、WebM 等格式
- 视频通过相对路径 `/videos/文件名` 引用，无需修改代码中的域名
- 若暂时没有视频文件，页面仍可正常访问，播放器会显示 poster 封面图
